---
source_handle: codex-cli-local-app-server-schema
fetched: 2026-08-14
source_path: .research/source-captures/codex-cli-0.147.0/schema/ServerNotification.json
provenance: source-direct
substrate_confidence: source-direct
---

## Summary

The generated `ServerNotification.json` from the local `codex-cli 0.147.0` build gives the current concrete app-server notification schema for this environment. It enumerates `ThreadActiveFlag` values `waitingOnApproval` and `waitingOnUserInput`, thread status variants `notLoaded`, `idle`, `systemError`, and `active`, and turn status values `completed`, `interrupted`, `failed`, and `inProgress`.

## Key passages

- [1] Lines 3905-3910: `ThreadActiveFlag` is an enum with `waitingOnApproval` and `waitingOnUserInput`.
- [2] Lines 5158-5227: thread status is a tagged union containing `notLoaded`, `idle`, `systemError`, and `active`; the `active` form requires `activeFlags`.
- [3] Lines 5573-5580: `TurnStatus` is an enum with `completed`, `interrupted`, `failed`, and `inProgress`.

## Structural notes

- This file was generated by the local CLI via `codex app-server generate-json-schema --out`.
- It is a version-specific machine-readable contract for the installed build, not a prose example.
