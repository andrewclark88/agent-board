# Isolated campaign evaluation

## 1. Coverage

The synthesis fully addresses the engagement seed:

- Defines outcome symmetry as shared glyph semantics and operator workflow,
  while preserving provider-specific evidence authority, confidence,
  capabilities, and control semantics.
- Identifies a smallest honest architecture: retain Codex's managed
  app-server/remote-TUI topology and add a Claude ordinary-TUI launcher with
  observation-only plugin/hooks and a normalized adapter boundary.
- Provides a detailed lifecycle and input/attention matrix for both providers,
  including identity, working start, permission waits, other input, completion,
  interruption, failure, and process/session end.
- Evaluates Herdr as prior art without overstating its authority.
- Specifies a concrete live-validation matrix and semantic acceptance criteria.
- Names clear conditions for revisiting the decision.
- Explicitly protects ordinary interactive terminal use, qualifies inferred
  state, and prohibits treating generic terminal input as semantic approval.

No material seed facet is omitted or left unacknowledged.

## 2. Coherence

The document is internally coherent. Its central distinction—common product
semantics above asymmetric provider evidence—is introduced in the decision
position and consistently carried through the evidence matrix, capability
model, topology recommendations, Herdr analysis, validation requirements, and
revisit triggers.

The architecture follows from the evidence presented: Codex retains its
stronger native lifecycle surface; Claude preserves its ordinary TUI and uses
hooks where available; confidence and diagnostic metadata prevent weaker Claude
evidence from masquerading as equivalent authority. The `?` fallback,
Board-owned completion acknowledgement, and separation of semantic actions from
raw terminal control reinforce one another rather than introducing competing
contracts.

No material join-seam inconsistency is apparent.

## 3. Contradictions

An explicit `## Contradictions` section is present. It correctly distinguishes:

- the substantive `tension` between Codex's authoritative active/interrupted
  evidence and Claude's weaker prompt-start/interruption evidence; and
- the `qualifies` relationship between Herdr's generic reporting API and its
  shipped screen-derived integrations.

The named positions are preserved side-by-side and cited independently. The
synthesis does not smooth the provider asymmetry into false native-signal
convergence. Its product-level resolution changes the common contract's
scope—shared glyph meaning rather than shared confidence—without claiming the
underlying evidence has converged.

The disconfirming analysis is also substantive: it tests native-signal parity,
screen inference as the primary architecture, and an SDK-owned Claude topology.

## 4. Groundedness

The citation posture presents as disciplined and source-grounded:

- Provider-specific lifecycle, hook, session, SDK, plugin, and Herdr claims
  carry source-handle citations.
- Composed product conclusions and architectural recommendations are visibly
  marked with `inferred` or `extends`.
- The synthesis distinguishes source evidence from product decisions and
  explicitly identifies specialist analyses as lenses rather than citation
  targets.
- No absolute effort estimates, comparative superlatives, or uncited
  named-feature claims materially drive the conclusion.
- Known uncertainty is preserved around Claude working-start ordering,
  ordinary-TUI question visibility, interruption recovery, background work,
  and extension availability.

The isolated read reveals no claim that urgently requires downstream
citation-chain spot-checking. The actual attestation-chain integrity remains
outside this evaluator's permitted scope.

## 5. Recommendations

The recommendations are concrete, proportionate, and priority-aware in
substance:

1. Preserve the normalized glyph and workflow contract.
2. Retain the existing Codex topology.
3. Add a narrowly scoped, observation-first Claude launcher/plugin/translator.
4. Expose provider capabilities, evidence confidence, and diagnostic fallback.
5. Require the cross-provider live-validation matrix before advertising
   symmetric support.
6. Revisit the decision only when provider contracts, validation results, or
   product control scope materially change.

They directly support the seed's preference for outcome symmetry while
preserving the stated honesty guardrails.

## Verdict

**APPROVED**

The synthesis is complete, coherent, visibly qualified, and appropriately
grounded from the perspective available to this isolated evaluation.
